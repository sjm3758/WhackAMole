function Button(text, xPos, yPos, width, height) {
    //string the button will display
    this.text = text;
    this.xPos = xPos;
    this.yPos = yPos;
    this.width = width;
    this.height = height;
}